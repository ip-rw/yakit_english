export enum EngineRemoteGV {
    /**
     * @name Initiate Engine Version Check
     * Engine Plugin: Determine if current version is higher than compatible software version; if lower, trigger engine version check popup
     */
    RemoteCheckEngineVersion = "remote-check-engine-version"
}
